{
	"jobName": "$job-name",
	"jobQueue": "$job-queue",
	"jobDefinition": "$job-definition",
	"containerOverrides": {
		"resourceRequirements": ["here you can specify resource reqs for VCPU and MEM"], 
		"command": ["this maps to docker CMD, here we'd put in the flags we want to use with stress-ng"]
	}
}
